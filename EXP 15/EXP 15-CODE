#include <reg51.h>

void main(void)
{
    unsigned char hexa = 0xFF;
    unsigned char hundreds, tens, units;

    ACC = hexa;

    B = 10;
    units = ACC % B;
    ACC = ACC / B;

    B = 10;
    tens = ACC % B;
    ACC = ACC / B;

    hundreds = ACC;

    P0 = units;
    P1 = tens;
    P2 = hundreds;

    while(1);
}
